'use strict';

describe('myApp.reshopping module', function() {

  beforeEach(module('myApp.reshopping'));

  describe('reshopping controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});